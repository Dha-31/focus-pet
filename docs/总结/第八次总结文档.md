# Focus Pet 第八次总结文档（项目最终总结 · v4.0.4 定稿）

> 日期：2026-08-17
> 性质：**项目收官总结**（最后一次总结文档；v4.0.4 即最终版本，不再规划后续版本）
> 上一篇：docs/总结/第七次总结文档.md
> 关联：docs/设计文档.md、docs/README.md、docs/GitHub桌宠调研报告.md、docs/Open-LLM-VTuber借鉴分析.md

---

## 一、项目回顾：我们在做什么

**Focus Pet —— 陪你学习、也监督你学习的桌面养成宠物。**

它不是一个普通桌宠：它把"学习监督"和"陪伴养成"揉进一只会动、会生气、会撒娇的小猫里。
你专心学习时它在旁边卖萌、打盹、攒好感；你一摸鱼它就一点一点变生气，用分级阻断把你拉回来；
它还会陪你养成好感、买家具、装饰自己的空间、拿每日/每周/每月奖章——把枯燥的自律变成"养宠物"。

从 v1 到 v4.0.4，项目经历了 8 个大阶段、几十次迭代，最终从"纯 Python 命令行小工具"
长成了"Electron 透明 HTML 桌宠 + 浏览器扩展 + 本地 AI 感知"的完整桌面应用。
**v4.0.4 即最终版本，功能就此定稿，不再规划后续版本。**

---

## 二、版本历程全景

| 版本 | 主题 | 核心成果 |
|---|---|---|
| **v1** | 桌宠与监督 | 透明置顶小猫、情绪温度计、活动窗口检测、黑白名单+教宠物、分级阻断、番茄钟、分心日志 |
| **v1.5** | 扩展与皮肤 | Chrome/Edge 扩展（URL 级拦截）、任意图片一键做皮肤（AI 抠图）、本地桥接服务 |
| **v2** | 摄像头监督 | 人脸/低头/手部检测、离屏学习、疑似玩手机、人不在提醒（**最终版已删除**） |
| **v2.5** | 截图画面分析 | 本地 OCR + 图案/颜色 + ML 分类器，多信号兜底判定，全程本地不保存不上传 |
| **v3** | 养成系统 | 专注升级/好感度/多档模式/用户自定义形象（状态机皮肤） |
| **v3.1** | 商店与空间 | 专注币经济、家具商店、我的空间、自定义形象自动适配 |
| **v3.2** | 成就与报表 | 成就徽章、专注数据报表、"你在看我吗"、四档模式、防绕过强化、扩展上架准备 |
| **v3.3** | 开发者编辑器 | 集中配置中心、数据驱动编辑器、热更新、导出导入 |
| **v3.5** | 状态机与主题 | 事件→状态→动画状态机、主题资源包、庆祝/出错/睡觉动画、托盘、单实例、位置记忆 |
| **v3.6** | 陪伴感 | 音效、免打扰 DND、迷你模式、活动驱动动画（打盹/踱步）、眼睛追踪、全局快捷键 |
| **v3.7** | 新手引导 | 首次启动欢迎、数据驱动帮助中心、全窗口 UI 现代化 |
| **v4.0.1** | 互动玩法 | 投喂、每日打卡、空间场景切换（打工/摄像头按用户要求随后删除） |
| **v4.0.4** | **交互界面升级（最终版）** | **Electron 透明 HTML 桌宠正式落地、全部窗口 HTML 化、奖章墙、空间/商店大改、摸头改双击、无条件启动问候** |

> 帮助中心版本号已随最终版更新为 `4.0.4`（`core/help_data.py`）。

---

## 三、当前版本功能全景（v4.0.4 · 最终版）

### 监督与阻断
- 活动窗口检测 + 浏览器扩展 URL 级黑白名单 + 教宠物（误判可纠正，桌宠会记住）
- 本地截图画面分析（OCR/图案/颜色/ML）作为兜底，**可强制拦截**，用户可点"这是学习页面"解封
- 情绪温度计逐级变生气（4 档）、分级阻断（提醒→遮挡→提示关闭），考试/日常/轻松/自定义四档模式
- 学习计时、番茄钟、退出承诺锁、防绕过扣币扣好感

### 桌宠体验（Electron HTML）
- 透明 + 置顶 + 鼠标穿透的 HTML 小猫：呼吸、眨眼、情绪、抖动、打盹、踱步、眼睛追踪
- **说话气泡**：紧贴脑袋右侧、最大字号 12px、随迷你模式缩放、绝不两字换行、不遮脸
- 双击摸头（冒爱心+脸红+眯眼，带冷却）、投喂、每日打卡
- 右键菜单（分类分组、限高滚动）、系统托盘（退出=最小化到托盘）、开机自启、迷你模式、免打扰

### 养成与空间
- 专注币经济：学习 5 分钟 = 1 币 + 每日打卡 20 币（连签加成），成就/奖章 30 币；家具 450~1900 币，按每天专注 6~7 小时校准（小件 4~5 天、主流约 1.5~2 周、高档约 2.5~3 周）
- 奖章系统（苹果健身 App 风格）：每日/每周/每月 9 枚 + 顶部进度条
- 成就系统：11 个里程碑徽章
- 我的空间：温馨小屋 / 星空田野 / 海边 / 森林 四张地图，家具 110px 自由拖拽、猫 110px 最上层
- 商店：家具按地图分类（含 6 件露营家具），分地图购买/摆放，价格可配置

### 全部窗口 HTML 化
- 设置 / 黑白名单（加宽 1500）/ 帮助中心 / 成就（奖章墙）/ 学习报告 / 商店 / 我的空间 共 7 个 HTML 窗口
- Tk 仅作为 Electron 不可用时的回退渲染器

---

## 四、技术架构

```
Focus Pet
├── main.py                 # 入口：Electron 优先，失败回退 Tk
├── desktop/                # Electron 壳（main.js/preload.js）—— 透明+置顶+穿透+托盘
├── ui/web_pet/             # HTML 桌宠 + 7 个 HTML 辅助窗口 + Electron 桥
├── ui/                     # Tk 回退版（PetApp/空间/商店/设置编辑器等）
├── core/                   # 配置/规则/情绪/会话/番茄钟/日志/经济/奖章/成就/皮肤/自启
├── sensors/                # 窗口检测 / 截图画面分析
├── blockers/               # 分级阻断
├── bridge/                 # 本地桥接（扩展 ↔ 主程序，127.0.0.1）
├── browser_extension/      # Chrome/Edge 扩展（MV3）
├── tools/                  # 皮肤生成/主题脚手架/打包 build_exe.py
└── data/                   # 配置与用户数据（LOCALAPPDATA 稳定化）
```

---

## 五、关键技术决策与踩坑记录（最重要的经验）

1. **渲染方案三选一，最终选 Electron**
   - pywebview / CEF 在 Python 3.14 下不可行；Tk 透明与动画能力有限。
   - **Electron 43.2.0** 透明桌宠方案胜出（学 Open-LLM-VTuber 桌宠模式），`main.py` 优先 Electron、失败回退 Tk。

2. **透明窗口 + 无独显 = 软件渲染，踩了两个大坑**
   - 必须 `app.disableHardwareAcceleration()` 才能真透明（否则黑屏）。
   - **合成层坑**：气泡若被 `will-change/translateZ` 提升成独立合成层，软件合成器会把它丢弃（屏幕上看不见）；普通 div（如右键菜单）则正常。**教训：透明窗口里别随便做层提升。**
   - **宽度坑**：绝对定位气泡放进 150px 小区域会因"可用宽度=容器宽-左偏移"而一行只剩一两个字、变成细长白柱。**教训：定位前先算可用宽度。**

3. **"没说话"的真相往往是触发条件**：`main.py` 启动提醒只在"今天未打卡"时触发，用户打过卡后启动就一句不说 → 后来改为**无条件启动问候**。

4. **分界线作用域 bug**：`fy` 在 `draw()` 局部定义却被 `drawWallDeco()` 引用，导致星空场景的山丘一直画不出来（报错被吞）。**教训：跨函数共享的常量提到模块级。**

5. **Live2D 评估后弃用**：Cubism 运行时加载问题 + 用户上传任意图片难以自动绑骨骼 + 云端模型体积问题 → 最终版不包含，保留经典/HTML 两种形象模式。

6. **主动借鉴优秀案例**：从 Open-LLM-VTuber 借鉴了桌宠窗口方案（透明/穿透/托盘）；GitHub 调研报告驱动了渲染选型。**先调研再动手，少走弯路。**

7. **功能做减法**：按用户反馈删除了打工、摄像头监督、饰品、小游戏、AI 对话——"学习软件，不要小游戏"，最终版保持精简。

8. **隐私承诺贯穿始终**：摄像头/截图本地处理不上传；浏览器扩展只连 127.0.0.1；数据目录 `%LOCALAPPDATA%\FocusPet` 稳定化。

---

## 六、测试与质量保障

- 每次改动：`py_compile` + `python main.py --headless-check` + JS 语法校验 + Electron 渲染冒烟（截图/像素验证）
- 关键 UI 问题（气泡渲染、层级、森林场景）都用**真实屏幕截图 + 像素分析**验证后才交付
- 改一处必查影响面：气泡位置连带宽度/避让，分界线改动连带星空/森林两场景
- 临时诊断代码交付前全部清理（本次已清理 90+ 个调试文件）

---

## 七、本版说明（最终版定稿）

1. **当前形态**：以源码版运行（`start_pet.bat` / `demo_electron_pet.bat`）；Electron 透明 HTML 桌宠 + 7 个 HTML 窗口，Tk 仅作回退。
2. **客户端与安装包（已完成）**：`tools/build_client.ps1` 一键产出精简版客户端（保留屏幕分析 OCR，排除重型依赖）+ Inno Setup 安装包（`release/FocusPet-Setup-4.0.4.exe`，按用户安装、含开始菜单/桌面快捷方式/卸载程序）；已验证「安装→运行→卸载」全流程。
3. **运行时**：Electron 运行时（约 137MB）不提交 git，换机器需在 `tools/electron/` 重新放置。
4. **浏览器扩展**：上架 zip 已就绪，如需上架 Edge/Chrome 商店可自行用开发者账号提交。
5. **已决定不做（最终版不含）**：注册登录、界面大改版、生态扩展（创意工坊/插件 SDK/MOD/i18n）、AI 对话、Q 版生成、Live2D 形象——按用户决定，v4.0.4 即最终版本。
6. **曾尝试后删除**：打工、摄像头监督、饰品、小游戏。

---

## 八、结语与展望

Focus Pet 从一个"会变生气的小猫"开始，一步步长成了集**监督、陪伴、养成、空间、成就**于一体的桌面学习伴侣，
并最终定稿于 **v4.0.4**。

这一路最重要的不是堆了多少功能，而是**该砍的果断砍、该借鉴的主动借鉴、每一次改动都反复自查回归**——
让产品以"可运行、可体验、用户满意"的状态收官。

若未来某天心血来潮想继续养它，以下方向可供参考（**仅作备忘，非规划**）：
- 把小猫接到云端 AI，让它真正会聊天
- 用积累的专注/分心数据生成个性化学习建议
- 移动端远程监督 / 数据看板
- 账号体系与多设备同步

感谢每一位愿意把自律交给一只小猫的你。
愿小猫陪你专注的每一分钟，都变成看得见的成长。

**—— Focus Pet · v4.0.4 定稿 · 完结 ——**
