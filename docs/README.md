# Focus Pet 🐾

陪你学习、也监督你学习的桌面养成宠物。平时卖萌，你摸鱼时通过"情绪温度计"
逐级变生气，用动画和阻断机制管住你。

**当前状态：v4.0.4（最终版）**。Electron 透明 HTML 桌宠 + 全部窗口 HTML 化 + 浏览器扩展 + 本地屏幕分析；安装包已就绪，扩展已提交 Edge 商店审核。

## 功能一览

**v1 已实现**
- ✅ 桌宠：透明置顶小窗，上下浮动、眨眼、按情绪变表情（开心→好奇→不耐烦→生气→暴怒）
- ✅ 情绪温度计：分心越久越生气，回到学习慢慢消气
- ✅ 活动窗口检测：标题 + 进程名（ctypes，不截图、不保存）
- ✅ 判定规则：黑名单为主 + 白名单特例 + "教宠物"
- ✅ 分级阻断：Lv1 提醒 / Lv2 动画遮挡 / Lv3 最小化（Lv4 强制关闭默认关）
- ✅ 番茄钟开关（默认关；开=25/5 循环，休息窗口解除限制）
- ✅ 声明制：开始前告诉宠物"今天学什么"，结束给总结
- ✅ 分心日志 + 会话记录

**v1.5 新增**
- ✅ **浏览器扩展（Chrome/Edge）**：URL 级黑名单/白名单特例/教宠物；打开黑名单网站会跳转拦截页
- ✅ **图片皮肤**：任意图片一键做成桌宠（`tools/make_skin.py` 自动抠图/去底/缩放）
- ✅ **本地桥接服务**：扩展与主程序通过 `127.0.0.1` 本地通信，数据不出本机

**v2 新增**
- ✅ **摄像头监督**：人脸检测 + 低头判断 + 手部检测
- ✅ **离屏学习**：低头写字/看书也算专注（宠物安静陪伴）
- ✅ **疑似玩手机**：低头 + 手在脸附近 → 宠物提醒
- ✅ **人不在**：宠物提醒"你去哪了？"，不计专注
- ✅ 模型下载工具（`tools/fetch_models.py`）
- ✅ **摄像头设置窗口**（`python main.py --camera-setup`：实时预览 + 选择前置/后置 + 一键保存）

**v2.5 新增**
- ✅ **截图画面分析（多信号）**：窗口标题/URL 判定不了时，本地截屏分析
  - OCR 文字识别（离线中文）：读到"目录/课件/代码"→学习，"直播/弹幕/攻略"→分心
  - 图案/结构：视频播放器特征、代码编辑器（深色）、文档排版密度
  - 颜色/亮度统计 + 机器学习分类器（可训练、持续提升）
  - 全程本地，不保存不上传
- ✅ 自检命令 `python main.py --screen-check`

**v3 新增**
- ✅ **养成系统**：专注攒经验升级——Lv.4 戴皇冠、Lv.6 金色描边、越长越大；好感度随摸鱼下降
- ✅ **多档模式**：日常自习（宽松）/ 考试冲刺（更严格，更快升级阻断）
- ✅ **用户自定义桌宠**：右键"更换形象"切换皮肤、或导入你自己的图片当桌宠

**V3.1 新增（商店 + 个人空间）**
- ✅ **专注币**：专注 1 分钟 = 1 币，存到 `data/inventory.json`
- ✅ **商店**：右键 → "商店…"，买装饰品（帽子/蝴蝶结/眼镜/围巾/小花/皇冠）和家具（地毯/台灯/绿植/窗户/学习桌/书架/沙发）
- ✅ **我的空间**：右键 → "我的空间…"，宠物房间 + 摆放家具
- ✅ **自定义形象自动适配**：导入图片时自动识别人脸，帽子戴头上、眼镜戴眼睛上、生气时眉毛/腮红贴脸上

**V3.2 新增**
- ✅ **成就系统**：11 个成就徽章（累计专注/连续专注/等级里程碑等），解锁奖励专注币
- ✅ **专注数据报表**：右键"数据报表"→ 每日专注柱状图 + 每日分心柱状图 + 分心明细
- ✅ **"你在看我吗"**：鼠标靠近宠物时它盯着你看（瞳孔跟随 + 歪头 + 偶尔搭话）
- ✅ **多档模式细化**：轻松 / 日常 / 考试冲刺 / 自定义 四档
- ✅ **防绕过强化**：学习中途退出或连续输错退出码 → 扣金币和好感度
- ✅ **扩展上架准备**：图标 + 隐私政策 + 提交指南 + 打包 zip（`release/`），待你用开发者账号提交

**V3.3 新增（开发者编辑器 · 集中配置）**
- ✅ **集中配置中心**：所有可调参数统一存 `data/settings.json`，代码全部从这里读
- ✅ **数据驱动编辑器**：右键"设置…"→ 自动按清单生成界面（汇率/阻断阈值/经验表/商品价格/关键词 + 应用配置）
- ✅ **改一个值全项目生效**：保存即热更新，无需重启
- ✅ 导出/导入配置（备份与分享）、恢复默认
- ✅ 以后加新功能：在 `core/settings_schema.py` 清单加一条，编辑器自动多出输入框

**v3.5 新增（状态机 + 主题资源包 + 桌面体验）**
- ✅ **「事件→状态→动画」状态机**：情绪状态（开心/好奇/不耐烦/生气/暴怒）+ 瞬时状态（庆祝/出错）+ 睡觉
- ✅ **主题资源包**：一个皮肤目录 = 多个状态图（angry.png/furious.png/celebrate.png/error.png/sleep.png…），
  没有就回退 pet.png / 程序化小猫；兼容旧版单图皮肤
- ✅ **主题脚手架 + 校验**：`tools/theme_scaffold.py` 一张图生成主题骨架，`tools/validate_theme.py` 检查状态缺图
- ✅ **导入主题包 zip**：右键「导入主题包…」或 `python main.py --import-theme 包.zip`
- ✅ **完成庆祝 / 出错反馈 / 睡觉 Zzz / 脚下阴影**动画
- ✅ **系统托盘**：双击显示/隐藏、右键开始/结束学习/退出（纯 ctypes，零依赖）；**图标跟随当前皮肤**（自定义形象换皮后托盘一起变）
- ✅ **单实例锁**：防止开两个桌宠
- ✅ **位置记忆**：宠物拖到哪，下次启动还在这
- ✅ **轻量 UI 美化**：摄像头设置/更换形象窗口统一配色 + 粉色现代按钮

**v3.6 新增（陪伴感增强）**
- ✅ **提示音效**：开始学习/完成庆祝/分心提醒/出错/番茄钟到点（winsound 合成，可开关，免打扰自动静音）
- ✅ **免打扰 DND**：右键/托盘一键让宠物闭嘴静音，监督照常（仍检测、记录、阻断）
- ✅ **迷你模式**：桌宠缩成小形态不挡桌面（右键/托盘/Ctrl+Alt+M）
- ✅ **活动驱动动画**：专注久了宠物打盹（呼吸变缓、眼睛半闭），分心频繁时左右踱步
- ✅ **随机待机动作 + 持续眼睛追踪**：宠物会跳一下/张望/探头，眼睛一直跟着光标
- ✅ **全局快捷键**：Ctrl+Alt+S 开始/结束学习，Ctrl+Alt+H 显示/隐藏，Ctrl+Alt+M 迷你模式（可关）

**v3.7 新增（新手引导 + UI 现代化）**
- ✅ **首次启动欢迎引导**：第一次打开自动弹「使用帮助」（可勾选"下次不再显示"）
- ✅ **使用帮助中心**：右键桌宠 →「使用帮助…」，分页：功能一览（带"本版新增"标签）/ 快捷键 / 设置项 / 常见问题 / 关于
- ✅ **帮助随版本自动更新**：内容从 settings_schema / 热键登记 / FEATURES 数据源自动收集，不过期
- ✅ **UI 统一现代化**：商店 / 成就 / 个人空间 / 数据报表 / 设置编辑器统一粉色主题 + 标题条 + 现代按钮

**v4.0.1 新增（互动玩法）**
- ✅ **摸头**：单击桌宠本体，好感 +1，宠物开心回应（3 秒冷却防连点）
- ✅ **投喂**：右键 →「投喂…」，花专注币买小鱼干/牛奶/蛋糕，增加好感
- ✅ **打工**：右键 →「打工…」，宠物去上班赚专注币（头顶 💼 + 倒计时，时间到自动结算，等级越高赚越多）
- ✅ **每日打卡**：右键 →「每日打卡…」，每天领专注币，连续签到奖励递增
- ✅ **空间场景切换**：我的空间左上角可切 温馨小屋/星空书房/海边/森林

**v4.0.4 新增（交互界面升级 · 最终版）**
- ✅ **Electron 透明 HTML 桌宠正式落地**：透明 + 置顶 + 鼠标穿透，呼吸/眨眼/情绪/打盹/踱步/眼睛追踪；说话气泡紧贴脑袋右侧、最大 12px、绝不两字换行
- ✅ **全部窗口 HTML 化**：设置 / 黑白名单（加宽 1500）/ 帮助中心 / 成就（奖章墙）/ 学习报告 / 商店 / 我的空间
- ✅ **奖章系统**：每日/每周/每月奖章（苹果健身 App 风格）
- ✅ **空间/商店大改**：四张地图（温馨小屋/星空田野/海边/森林）+ 露营家具，家具 110px 自由拖拽、分地图购买与摆放
- ✅ **双击摸头**（冒爱心+脸红+眯眼，带冷却）、无条件启动问候
- ✅ **打包分发（已完成）**：`release/FocusPet-Setup-4.0.4.exe` 一键安装（按用户安装、开始菜单/桌面快捷方式/内置卸载）
- ✅ **浏览器扩展上架（已提交 Edge 商店审核）**：上架包 `release/browser_extension.zip`；完整步骤与隐私政策见 `docs/扩展上架指南.md` 

**已决定不做（最终版不含）**：注册登录、AI 对话、Q 版/Live2D 形象、生态扩展（创意工坊/插件 SDK/i18n）；**曾尝试后删除**：打工、摄像头监督、饰品、小游戏。

## 环境要求

- Windows 10/11 + Python 3.10+（v1 基础功能无需 pip 安装任何包）
- 浏览器拦截需要 Chrome 或 Edge（新版）

## 快速开始

> 普通用户直接用安装包：下载 `release/FocusPet-Setup-4.0.4.exe`（约 166MB，或 GitHub Releases 页）安装即用，无需 Python。

```powershell
cd 本项目目录
python main.py --headless-check   # 自检核心逻辑（可选）
python main.py                     # 启动桌宠
```

桌宠出现在右下角，可拖动；**右键**有菜单：开始学习 / 结束学习 / **多档模式** / 番茄钟开关 / **更换形象** / **商店** / **我的空间** / **成就** / **数据报表** / **设置** / 教宠物 / 退出。

其他命令：
```powershell
python main.py --camera-setup  # 打开摄像头设置窗口（图形界面）
python main.py --status   # 查看配置和最近记录
python main.py --log      # 查看分心日志时间线
```

## 启用浏览器拦截（推荐：装完客户端顺手装扩展）

客户端的**窗口检测 + 截图分析**不需要扩展即可拦截；想要**网址级精确拦截**（打开 B 站/抖音直接拦标签页），按下面三步：

1. **安装并启动 Focus Pet 客户端**
2. 右键桌宠 →「设置」→「应用配置」→ 确认**「浏览器扩展桥接」**为开启（默认已开；若关过，打开后重启客户端）
3. 在浏览器里安装扩展（**一键，无需开发者模式**，浏览器会自动更新）：
   - **Edge**：打开 https://microsoftedge.microsoft.com/addons 搜索 **Focus Pet** → 点「获取」
   - **Chrome**：打开 Chrome 网上应用店搜索 **Focus Pet** → 点「添加至 Chrome」
4. 装好后点扩展图标，应显示「已连接 Focus Pet（本机）」

> **开发者/审核前自测**（商店还没上架时）：也可以手动加载——打开 `chrome://extensions` 或 `edge://extensions` → 打开右上角「开发者模式」→「加载已解压的扩展程序」→ 选择本项目 `browser_extension` 文件夹。

生效效果：
- 打开黑名单里的网站（如 bilibili.com）→ 自动跳转到拦截页
- 拦截页点「我在学习这个页面！」→ 教宠物记住，下次放行（也可用扩展弹窗的「标记为学习」）
- 浏览器窗口本身不会被最小化/关闭（只拦标签页）
- 扩展只把当前标签页 URL 发给本机 127.0.0.1，不上传任何数据

## 黑名单 / 白名单（data/）

- `blacklist.json`：`urls`（域名/链接子串）、`processes`（进程名）、`titles`（标题关键词）
- `whitelist.json`：格式同上，**优先级高于黑名单**
- 示例（B 站上课）：黑名单 `urls` 写 `bilibili.com`，白名单 `urls` 写你课程视频的精确链接
- 被误判时：右键宠物"这个是学习用的！"，或扩展拦截页/弹窗里点"标记为学习"

## 皮肤系统（v1.5）

**一键生成皮肤**（推荐，自动抠图）：
```powershell
python tools/make_skin.py 你的图片.png 我的小猫
```
- 自动处理：rembg AI 抠图 →（不可用则）PIL 白色背景去除 + 缩放 → 直接复制
- 首次 AI 抠图需联网下载约 300MB BiRefNet 模型（网慢会久）。可提前预下载（带进度条）：
  ```powershell
  python tools/fetch_birefnet.py
  ```
- 不想用 AI 抠图时：`python tools/make_skin.py 你的图片.png 我的小猫 --no-rembg`（快速白色去底）
- 生成到 `skins/我的小猫/pet.png`，自动改配置，重启桌宠生效

**手动方式**：图片（透明 PNG 更好）存成 `skins/<名字>/pet.png`，把 `data/config.json` 的
`pet.skin` 改成 `<名字>`，重启。不配置图片就用内建程序化小猫。

## 启用摄像头监督（v2，可选）

> ⚠️ **最终版已移除**：摄像头监督在 v4.0.4 中已按用户要求删除（不再包含 opencv/mediapipe 依赖）。以下为历史记录，仅供旧版本源码参考。

**推荐用图形界面设置**（不用手改配置）：
```powershell
python main.py --camera-setup
```
窗口里能实时预览画面、切换前置/后置、显示"✅ 检测到人脸（推荐）"，点保存即可。

也可以手动配置：

1. 安装依赖（如果还没装）：
   ```powershell
   pip install opencv-python mediapipe
   ```
2. 下载模型（已下载过可跳过）：
   ```powershell
   python tools\fetch_models.py
   ```
3. 编辑 `data\config.json`，把 `"camera": { "enabled": false ... }` 改成 `"enabled": true`
4. 重启桌宠，控制台会显示"摄像头已启动（后端: mediapipe）"

自检（不开桌宠也能测，约 5-10 秒）：
```powershell
python main.py --camera-check
```
会打印每帧的判定：`person_present`（有人）、`head_down`（低头）、`hand_active`（手在动）、`off_screen_study`（离屏学习）、`phone_suspicion`（疑似玩手机）。

> 隐私说明：摄像头画面只在本地实时分析，**不保存、不上传**。

## 配置（data/config.json）

| 字段 | 说明 | 默认 |
|---|---|---|
| pomodoro.enabled | 番茄钟开关 | false |
| pomodoro.focus_minutes / break_minutes | 专注/休息时长 | 25 / 5 |
| blocking.tiers_seconds | 阻断升级阈值（秒） | [5, 15, 30, 60] |
| blocking.force_close_enabled | Lv4 强制关闭（慎开） | false |
| blocking.save_warning_seconds | 关闭前保存倒计时 | 10 |
| lock.enabled / lock.exit_code | 退出承诺锁 | false / "1234" |
| pet.skin | 皮肤名 | "default" |
| extension.enabled / extension.port | 浏览器扩展桥接 | false / 18765 |
| camera.enabled / device_index / interval_seconds | 摄像头开关 / 设备编号 / 采样间隔 | false / 0 / 1 |
| screen_analysis.enabled / interval_seconds | 截图分析开关 / 间隔(秒) | true / 10 |

## 项目结构

```
focus-pet/
├── main.py              # 入口：监督循环 + CLI
├── core/                # 配置 / 规则 / 情绪 / 会话 / 番茄钟 / 日志
├── sensors/             # 感知层（窗口检测 / 截图分析）
├── ui/                  # 桌宠窗口 + 皮肤
├── ui/web_pet/          # Electron HTML 桌宠（index.html + 7 个 HTML 窗口）
├── blockers/            # 分级阻断
├── bridge/              # 本地桥接服务（扩展 <-> 主程序）
├── browser_extension/   # Chrome/Edge 扩展（MV3）
├── tools/               # 辅助工具（make_skin.py）
├── data/                # 配置与数据（运行时生成日志）
├── site/                # 官网页面（GitHub Pages）
├── docs/设计文档.md      # 完整设计
└── skins/               # 皮肤资源包
```

## 升级路线

- **v1**：✅ 桌宠 + 情绪温度计 + 黑名单/特例 + 番茄钟 + 窗口检测 + 分级阻断 + 分心日志
- **v1.5**：✅ 浏览器扩展 + ✅ 图片皮肤 + ✅ 本地桥接
- **v2**：✅ 摄像头（人脸 + 视线 + 手部：离屏学习/玩手机）+ 摄像头设置窗口
- **v2.5（已完成）**：✅ 截图画面分析（本地，不保存不上传）
- **v3（已完成）**：养成进化 + 好感度 + 多档模式 + 用户自定义桌宠
- **v3.1（已完成）**：专注币商店 + 个人空间家具 + 自定义形象自动适配
- **v3.2（已完成）**：成就系统 + 数据报表 + "你在看我吗" + 四档模式 + 防绕过强化 + 扩展上架准备
- **v3.3（已完成）**：开发者编辑器（集中配置中心，数据驱动 + 实时热更新 + 导出导入）
- **v3.5（已完成）**：事件→状态→动画状态机 + 主题资源包（脚手架/校验/zip 导入）+ 分级动画 + 庆祝/错误/睡眠动画 + 系统托盘 + 单实例锁 + 位置记忆 + 轻量 UI 美化
- **v3.6（已完成）**：音效提示 + 免打扰 DND + 迷你模式 + 活动驱动动画（打盹/踱步）+ 随机待机/持续眼睛追踪 + 全局快捷键
- **v3.7（已完成）**：新手引导（首次启动欢迎页）+ 使用帮助中心（数据驱动，随版本自动更新）+ UI 全面现代化（商店/成就/空间/报表/设置统一主题）
- **v4.0.1（已完成）**：互动玩法（摸头/投喂/打工时钟/每日打卡/空间场景切换）
- **v4.0.4（当前 · 最终版）**：✅ 交互界面大改版（Electron HTML 桌宠 + 全部窗口 HTML 化 + 奖章墙 + 空间/商店大改）+ ✅ 打包分发（安装包）+ ✅ 扩展上架（已提交 Edge 审核）
- ~~v4.0.2 / v4.1 / v4.2~~：已按用户决定取消（AI 对话 / 生态扩展 / 注册登录等均不做），**v4.0.4 即最终版本**

升级方式是**增量修改**，不推倒重来。详细设计见 [`docs/设计文档.md`](docs/设计文档.md)。

## 常见问题

- **扩展显示"未连接"？** 确认 Focus Pet 正在运行，且 `data/config.json` 的
  `extension.enabled` 为 `true`（改完要重启桌宠）。
- **拦截页反复跳转？** 教宠物后仍被拦，说明 URL 没完全匹配白名单；白名单用
  精确链接（子串匹配），确认没写错。
- **强制关闭为什么默认关？** 会丢未保存内容，建议开启后配合保存倒计时使用。
- **没装 git？** 可安装 https://git-scm.com/ 后在本目录执行 `git init`（.gitignore 已备好）。