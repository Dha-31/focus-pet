# 如何添加一个皮肤

1. 在本目录（skins/）新建一个文件夹，例如 `skins/mycat/`
2. 把你的宠物图片（PNG，透明背景更好）命名为 `pet.png` 放进去
3. 修改 `data/config.json` 中的 `pet.skin` 为文件夹名，例如 `"mycat"`
4. 重启桌宠即可生效

说明：
- 想换回内建程序化小猫，把 `pet.skin` 改回 `"default"`
- 透明背景抠图可以先用在线抠图工具做好；自动抠图功能在 v1.5 加入
- 图片建议 200x200 以内，太大的图缩放功能后续版本加入