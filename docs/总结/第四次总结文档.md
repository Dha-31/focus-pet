# Focus Pet 第四次总结文档（日志 #4）

> 日期：2026-08-15
> 阶段：v3.5 → v3.7.1（含 12 轮体验反馈修复）→ 客户端打包 → 路线图规划
> 上一篇：docs/总结/第三次总结文档.md

## 一、本次任务的目标

1. 完成 **v3.5**：事件→状态→动画状态机 + 主题资源包 + 桌面体验（托盘/单实例/位置记忆）+ UI 美化
2. 完成 **v3.6**：陪伴感增强（音效/免打扰 DND/迷你模式/活动驱动动画/随机待机/持续眼睛追踪/全局快捷键）
3. 完成 **v3.7**：新手引导/使用帮助中心（数据驱动、随版本实时更新）+ UI 现代化
4. **打包可双击运行的 Windows 客户端**（PyInstaller，体验版）
5. 根据用户连续体验反馈完成 **12 轮修复（v3.7.1）**
6. 规划：v4.0 拆分子版本、v4.2 客户端交互界面大改版、每日打卡/空间小游戏入路线图

## 二、已经完成的内容

### v3.5（状态机 + 主题资源包 + 桌面体验）
- 「事件→状态→动画」状态机：情绪状态（开心/好奇/不耐烦/生气/暴怒）+ 瞬时（庆祝/出错）+ 睡觉
- 主题资源包：theme.json + 每状态一张图（angry/furious/celebrate/error/sleep…），回退链兼容旧皮肤
- 工具链：theme_scaffold.py 脚手架 / validate_theme.py 校验 / zip 导入（右键 + --import-theme）
- 系统托盘（纯 ctypes 零依赖）、单实例锁、位置记忆
- 图片自适应窗口、自定义图像适配原则（硬性约束）

### v3.6（陪伴感增强）
- 音效提示（winsound 合成，可开关、免打扰自动静音）
- 免打扰 DND（宠物闭嘴，监督照常）、迷你模式（锁定小尺寸）
- 活动驱动动画：专注>10 分钟打盹、分心频繁踱步
- 随机待机动作（跳/张望/探头）+ 持续眼睛追踪
- 全局快捷键：Ctrl+Alt+S 学习 / Ctrl+Alt+H 显隐 / Ctrl+Alt+M 迷你

### v3.7（新手引导 + UI 现代化）
- 首次启动欢迎引导（可勾选"下次不再显示"）
- 使用帮助中心：功能一览（本版新增标签）/快捷键/设置项/常见问题/关于
- help_data 数据源：设置说明从 settings_schema 自动收集、热键登记同步、功能条目带版本号 → **帮助随版本实时更新**
- UI 统一现代化：统一粉色主题 + 标题条 + 现代按钮；开启 DPI 感知（文字清晰）

### 客户端打包（体验版）
- tools/build_exe.py：一键打包（体验版 / --full 完整版）
- 报表 matplotlib 缺失优雅降级；rembg + BiRefNet 高质量 AI 抠图打包进客户端
- 数据目录稳定化：%LOCALAPPDATA%\FocusPet（更新客户端不丢皮肤/配置/日志）
- 桌宠默认 340×340、辅助窗口统一 1080×1000；猫矢量绘制随窗口等比放大不糊

### v3.7.1（12 轮体验反馈修复）
1. 通知屏蔽：学习时自动开启 Windows 专注助手（屏蔽微信/QQ 等通知、闪烁、声音）
2. 窗口管理：辅助窗口不置顶/不模态；打开新窗口自动关闭旧的
3. 右键菜单改版（模式/形象/娱乐/系统分组 + 勾选式开关）+ DPI 清晰
4. 调整大小窗口（滑块 + 实时预览）
5. 修复 5 个窗口缩进损坏导致"菜单打不开"的严重 bug（全项目语法复查）
6. 小猫 340 + 界面调大第一眼可见 + 抠图升级 BiRefNet
7. 猫绘制基准固定 170（窗口大了猫真正变大）+ 界面 1080×1000 + rembg 打包
8. 大小记忆（修复读取顺序 bug）+ 删除"祭坛舞台"脚下阴影 + 数据目录稳定化 + 帮助白底建议
9. 自定义图像自适应窗口（修复"重启后消失"）
10. 说话气泡贴角色脑袋右边 + 随图像缩放
11. 气泡文字自动换行 + 高度自适应（不再溢出白框）
12. 气泡按文字实际宽度自适应 + 自动避让不遮挡图像

### 规划（已入文档）
- v4.0 拆分为 v4.0.1~v4.0.4 子版本（避免大版本风险）
- 新增 v4.2：客户端交互界面大改版（学习 GitHub 优秀案例 + 渲染方案评估）
- 每日打卡签到、空间场景切换/二维小世界 → v4.0.1

## 三、已修改的文件

本轮共 **39 个文件，+3335 / -320**（不含 research/、dist/ 等忽略目录）：

**新增（14）**：core/state_machine.py、core/theme.py、core/single_instance.py、core/sounds.py、core/hotkeys.py、core/focus_assist.py、core/paths.py、core/help_data.py、ui/window_manager.py、ui/size_dialog.py、ui/help_window.py、ui/tray.py、ui/theme_ui.py、tools/theme_scaffold.py、tools/validate_theme.py、tools/build_exe.py、docs/GitHub桌宠调研报告.md

**修改（22）**：main.py、core/config.py、core/settings_schema.py、ui/pet_app.py、ui/pet_renderer.py、ui/shop_window.py、ui/achievements_window.py、ui/report_window.py、ui/settings_editor.py、ui/space_window.py、ui/camera_setup.py、blockers/blocker.py、tools/make_skin.py、data/config.json、.gitignore、docs/README.md（原根 README.md 移入）、docs/设计文档.md、docs/总结/第一次/第二次/第三次总结文档.md、docs/第二次总结未来目标.png（移入 docs/总结/）

## 四、每个文件修改了什么

| 文件 | 修改内容 |
|---|---|
| core/theme.py | 主题资源包：theme.json 清单、状态→图片回退链、zip 导入、校验 |
| core/state_machine.py | 事件→状态→动画状态机（情绪+瞬时+睡觉） |
| core/single_instance.py | Windows 命名互斥量单实例锁 |
| core/sounds.py | winsound 合成音效（start/celebrate/alert/error/break）+ 静音开关 |
| core/hotkeys.py | RegisterHotKey 全局快捷键（Ctrl+Alt+S/H/M） |
| core/focus_assist.py | 读写注册表 QuietHours 控制 Windows 专注助手（通知屏蔽） |
| core/paths.py | 稳定数据目录：源码=项目目录；打包=%LOCALAPPDATA%\FocusPet（自动补齐默认文件） |
| core/help_data.py | 帮助数据源：VERSION + FEATURES（带 added_in）+ 设置项/热键收集 |
| core/config.py | 新增默认配置：mini_mode/dnd/sound/hotkeys/first_run/focus_assist/pet_size |
| core/settings_schema.py | 新增对应配置项（编辑器自动出现） |
| ui/pet_app.py | 桌宠主窗口（最大改动）：状态机驱动、主题按状态取图、DPI 尺寸、大小记忆、位置记忆、迷你/免打扰、活动动画、随机待机、眼睛追踪、菜单分组勾选、更换形象/调整大小入口、气泡自适应避让 |
| ui/pet_renderer.py | 程序化小猫 view_scale/napping、庆祝/错误/睡眠特效、fit_photo 图片自适应 |
| ui/tray.py | 系统托盘：Shell_NotifyIconW（shell32）、右键菜单、图标跟随皮肤 |
| ui/window_manager.py | 辅助窗口单例管理（打开新的自动关旧的） |
| ui/size_dialog.py | 调整桌宠大小：滑块 50%~300% + 实时预览（线性比例） |
| ui/help_window.py | 帮助中心 5 分页 + 欢迎引导 + 白底建议 FAQ |
| ui/theme_ui.py | 统一风格常量 + add_header/accent_button/card + AUX_SIZE |
| ui/shop/achievements/report/settings_editor/space/camera_setup | 统一主题风格、窗口尺寸、空间窗口按尺寸重绘、报表降级 |
| blockers/blocker.py | 分心提醒音效 |
| tools/make_skin.py | 抠图升级：BiRefNet 优先→u2net→四角扩散去背景 |
| tools/theme_scaffold.py / validate_theme.py | 主题骨架生成 / 状态缺图校验 |
| tools/build_exe.py | PyInstaller 打包（数据目录收集、rembg 打包、体验/完整模式） |
| data/config.json | 位置记忆（window_x/y/pet_size） |
| docs/* | README/设计文档/三次总结/调研报告更新 |

## 五、已运行的测试及结果

| 测试 | 结果 |
|---|---|
| python main.py --headless-check（多次，含 exe 版） | ✅ 全部通过（核心逻辑 + 状态机 + 主题 + 单实例） |
| GUI 冒烟：桌宠窗口 + 6 个辅助窗口逐个打开/关闭 | ✅ 全部正常，初始 1080×1000 |
| 托盘创建/销毁、右键菜单项、3 个热键注册 | ✅ 全部成功 |
| 气泡 bbox 严格验证（文字在框内/不挡图像/在窗口内） | ✅ 长/短文本均通过 |
| 桌宠缩放模拟（200→260）、大小记忆（500→重启读取） | ✅ 通过 |
| 自定义皮肤 40 帧动画无异常 | ✅ 通过 |
| 四角扩散去背景单元测试（角落透明、前景保留） | ✅ 通过 |
| 客户端 exe：--headless-check / --status / GUI 8-10 秒存活 | ✅ 通过 |
| %LOCALAPPDATA%\FocusPet 数据目录生成与默认文件补齐 | ✅ 通过 |

## 六、当前仍存在的问题

1. 客户端 exe **首次 AI 抠图需联网下载约 300MB BiRefNet 模型**（网慢会久；失败自动回退四角扩散）
2. 体验版客户端**未打包摄像头/截图分析**（mediapipe 等 AI 模块），需 `python tools/build_exe.py --full` 出完整版
3. 修复八之前旧 exe 导入的皮肤在 exe 内部，**更新后需重新导入一次**（新版已用稳定目录，不会再丢）
4. **docs/总结 里「第二次总结未来目标 - 副本.png」被删除**（用户整理文档时），是否恢复待确认
5. v4.0 系列尚未开始；v4.2 界面大改版（渲染方案评估）待做
6. 气泡等 UI 细节可能仍有未发现的观感问题，待用户继续体验反馈

## 七、下一步应该从哪里继续

1. **用户继续体验 v3.7.1 客户端**，有问题继续修复（当前主线）
2. 体验满意后 → **v4.0.1**：互动玩法（摸头/投喂/工作时钟/小游戏）+ 每日打卡 + 空间场景切换
3. 可选：出**完整版客户端**（含摄像头/截图分析）：`python tools/build_exe.py --full`
4. 后续按路线图：v4.0.2 AI 对话 → v4.0.3 Q 版生成 → v4.0.4 Live2D → v4.1 生态 → v4.2 界面大改版
5. 每次改代码后：跑 `python main.py --headless-check` + 全窗口冒烟 + 语法复查，再打包

## 八、禁止再次修改的内容

1. **自定义图像适配原则**（设计文档硬性约束）：任何新功能必须对自定义图像可用（一张图=可用桌宠，回退链，人脸元数据定位，特效画在形象外部）
2. **数据目录稳定化**：打包后数据/皮肤在 `%LOCALAPPDATA%\FocusPet`，**禁止改回 exe 内部**（_internal），否则更新丢数据
3. **隐私承诺**：摄像头/截图本地处理不上传；浏览器扩展只连 127.0.0.1
4. **帮助中心数据驱动机制**：help_data 是单一事实来源，禁止改回手写死文案
5. **用户已确认的偏好**：桌宠默认 340×340；辅助窗口 1080×1000 且不置顶/不模态；脚下阴影不要；迷你模式锁定小尺寸；调整大小走窗口（滑块+预览）不走边缘拖拽；气泡贴角色且不挡图像
6. **打包策略**：build/、dist/、*.spec 已进 .gitignore，**禁止提交**；不要用 --noconsole 隐藏错误输出（便于反馈）
7. **大版本拆分策略**：v4.0 保持 v4.0.1~v4.0.4 拆分，禁止合并回一个大版本
8. **用户数据文件**（data/ 配置与日志、皮肤、docs 截图备份）：禁止误删/误提交；如要删除用户文件必须确认
